{
  "extends": ["next/core-web-vitals", "next/typescript"],
  "rules": {
    "react/display-name": "off",                 // Disable the rule for display names
    "@typescript-eslint/no-unused-vars": "warn"  // Change to "warn" instead of "error" if you want to see warnings only
  }
}
